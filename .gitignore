build/
*.o
*.a
*.wasm
*.js.map
node_modules/
.DS_Store
.vscode/
.idea/
*.swp
.env
.env.*
CLAUDE.md

# Additional build directories
build-debug/
build-*/

# In-tree CMake artifacts (defensive: builds should use build-* dirs)
CMakeCache.txt
CMakeFiles/
cmake_install.cmake
CTestTestfile.cmake
_deps/
compile_commands.json

# Emscripten extra outputs
*.data
*.mem

# Editor / LSP caches
.cache/
.ccls-cache/

# OS junk
Thumbs.db
ehthumbs.db

# Local Emscripten SDK, if vendored into the tree
emsdk/

# Native debug bundles (macOS)
*.dSYM/

# Profanity source list (LDNOOBW English word list). Kept locally only; the
# build consumes the committed, pre-folded src/settings/username_denylist_data.hpp,
# never this raw file. Do NOT ignore that generated header — it is a committed
# build input with no build-time regenerator.
en.txt
